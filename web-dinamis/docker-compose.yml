# Layanan database
services:
  db-webdinamis:
    image: mariadb:lts
    environment:
      MARIADB_ROOT_PASSWORD: password!123
      MARIADB_DATABASE: dbcompro_nim
      MARIADB_USER: userwebdinamis_nim
      MARIADB_PASSWORD: O)xz6GWEwDOx1Ea9
    volumes:
      - db_data:/var/lib/mysql
      - ./db_data/dbcompro_nim.sql:/docker-entrypoint-initdb.d/dbcompro_nim.sql:ro
    restart: always

  container-statis:
    image: irawatiiskandar/himafor_nim:latest
    ports:
      - "80:80"
    restart: always

  container-dinamis:
    image: irawatiiskandar/web-dinamis:latest
    ports:
      - "3000:3000"
    restart: always
    environment:
      DB_USER: userweb_2388010025
      DB_PASSWORD: Iwv(0N3Edj-Tm2*b
      DB_NAME: dbcompro1
      DB_HOST: db-webdinamis
      NEXTAUTH_SECRET: ini-kata-kata-bebas-saja-32-karekter
      NEXTAUTH_URL: http://18.141.160.18
    depends_on:
      - db-webdinamis

volumes:
  db_data: {}
