# Layanan database
services:
  db-webdinamis:
    image: mariadb:lts
    environment:
      MARIADB_ROOT_PASSWORD: password!123
      MARIADB_DATABASE: dbcompro1
      MARIADB_USER: userweb_2388010025
      MARIADB_PASSWORD: Iwv(0N3Edj-Tm2*b
    volumes:
      - db_data:/var/lib/mysql
      - ./db_data/dbcompro1.sql:/docker-entrypoint-initdb.d/dbcompro1.sql:ro
    restart: always

  container-statis:
    image: irawatiiskandar/himafor_2388010025:latest
    ports:
      - "80:80"
    restart: always

  container-dinamis:
    image: irawatiiskandar/web-dinamis:latest
    ports:
      - "3000:3000"
    restart: always
    environment:
      DB_USER: userweb_2388010025
      DB_PASSWORD: Iwv(0N3Edj-Tm2*b
      DB_NAME: dbcompro1
      DB_HOST: db-webdinamis
      NEXTAUTH_SECRET: ini-kata-kata-bebas-saja-32-karekter
      NEXTAUTH_URL: http://18.141.160.18
    depends_on:
      - db-webdinamis

volumes:
  db_data: {}
